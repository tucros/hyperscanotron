<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <!-- Declare variables if needed -->
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Display the current storage path -->
        <TextView
            android:id="@+id/currentStoragePathTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/current_storage_path"
            android:textSize="18sp"
            android:textStyle="bold" />

        <!-- Button to change storage path -->
        <Button
            android:id="@+id/changeStorageButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="@string/change_storage_location" />

    </LinearLayout>

</layout>
